[package]
name = "ai-authoring-linter"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
clap = { version = "4.5", features = ["derive"] }
sha2 = "0.10"
regex = "1.10"

[dev-dependencies]
serde_json = "1.0"
