{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schema:HorrorPlace-Neural-Resonance-Lab:bci-geometry-bindings-collection-v1.json",
  "title": "BCI Geometry Bindings Collection v1",
  "description": "Array schema for loading packs of bci-geometry-binding-v1 objects. Used by lab tools and CI for batch validation.",
  "type": "array",
  "minItems": 1,
  "uniqueItems": true,
  "items": {
    "$ref": "schema:HorrorPlace-Constellation-Contracts:bci-geometry-binding-v1.json"
  },
  "description_note": "bindingId values must be unique across the collection; enforced by narrative requirement, not JSON Schema."
}
